import java.util.ArrayList;
import java.lang.Math;
public class suche {
    int[] Zahlen ={1,2,3,4,5,6,7,8,9};
    int links = 0;
    int rechts = Zahlen.size()-1;
    int mitte = Math.round((links+rechts)/2);
    while(links <= rechts) {
        
    }

}